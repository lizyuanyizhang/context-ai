export default {
  plugins: {
    // Tailwind CSS：原子化 CSS 框架，通过类名快速构建 UI
    tailwindcss: {},
    // Autoprefixer：自动添加浏览器前缀（如 -webkit-, -moz-）
    // 确保 CSS 在不同浏览器中都能正常工作
    autoprefixer: {},
  },
}
